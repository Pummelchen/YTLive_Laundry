# Secrets - stream.env holds the live YouTube stream key (chmod 600). Never commit it.
conf/stream.env

# Runtime logs
log/

# Generated: holds absolute paths from whichever machine built it
conf/playlist.txt

# Generated CCTV still of the premises, refreshed at every publisher start
conf/golden.jpg

# The music itself: 328MB of audio, not source
MP3/

# Local editing debris
.DS_Store
*.bak-*
